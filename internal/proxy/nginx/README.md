# Nginx Adapter

Not implemented in v0.1. Will be added in a future version.

The Nginx adapter will implement `proxy.ProxyAdapter` and generate
nginx.conf files with `server` blocks equivalent to the Caddy adapter.
